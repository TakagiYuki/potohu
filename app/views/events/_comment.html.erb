<!-- コメント一覧 -->
<% event_comments.each do |event_comment| %>
<div class="col-lg-12">
  <table class="table table-hover table-inverse">
    <tbody>
      <tr>
        <td><%= attachment_image_tag(@user, :image, :fill, 50, 50, fallback: "no_image.jpg", size: "50x50") %><br>
          <%= event_comment.user.name %>
        </td>
        <td><%= event_comment.comment %></td><br>
          <%#= form_with model:[@event, @event_comment], data: {remote: true} do |f| %>
          <%#= f.text_area :comment, class: "form-control",placeholder: "コメントをここに" %>
      <div class="modal-footer">
        <%#= f.submit "投稿する", class:"btn btn-default" %>
      </div>
      <%# end %>
      </tr>
    </tbody>
  </table>
<% end %>