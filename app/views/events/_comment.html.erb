
<% event_comments.each do |event_comment| %> <!-- コメント一覧 -->
<div class="col-lg-12">
  <table class="table table-hover table-inverse">
    <tbody>
      <tr>
    <td>
      <%#= link_to user_path(book_comment.user), class: "user_#{book_comment.user.id}" do %>
        <%= attachment_image_tag(@user, :image, :fill, 50, 50, fallback: "no_image.jpg", size: "50x50") %><br>
        <%= event_comment.user.name %>
      <%# end %>
    </td>
    <td><%= event_comment.comment %></td><br>
    
    <td><%= event_comment.comment %></td>
    <% if event_comment.user == current_user %>
      <td><%= link_to 'Destroy', event_event_comment_path(event_comment), class: 'btn-sm btn-danger', method: :delete, remote: true %></td>
    <% end %>
      </tr>
      </tbody>
      <% end %>
      </table>

