<% provide(:title, 'タグ作成') %>
<div class="container">
  <div class="row">
    <div class="col-md-8 col-md-offset-2">
      <h3>タグ作成</h3>
      <hr>
    </div>
  </div>
  <!-- 入力フォーム -->
  <%= render 'admin/tags/tag_form', url: admin_tags_path %>
</div>

<div class="col-md-8 col-md-offset-2">
  <table class="table table-hover table-inverse">
    <%= flash[:success] %>
    <thead>
      <tr>
        <th>image</th>
        <th>エリア名</th>
        <th>編集</th>
        <th>消去</th>
      </tr>
    </thead>
    <tbody>
      <% @tags.each do |tag| %>
      <tr>
        <td class="text-center"><%= attachment_image_tag(tag, :image, fallback: 'no_image.jpg', size: '90x60')%></td>
        <td><%= tag.name %></td>
        <td><%= link_to "編集", edit_admin_tag_path(tag), class: "btn btn-primary" %></td>
        <td><%= link_to "消去", admin_tag_path(tag), method: :delete, class: "btn btn-primary", "data-confirm" => "本当に削除しますか？" %></td>
      <% end %>
    </tbody>
  </table>
</div>
