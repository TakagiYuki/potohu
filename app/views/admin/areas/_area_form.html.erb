<div class="row">
  <div class="col-md-8 col-md-offset-2">
    <%= form_with(model: @area, url:url, remote: true) do |f| %>
      <%#= render 'shared/error_messages' %>
      <%= f.label :都道府県 %>
      <%= f.text_field :name, class: 'form-control' %>

      <%= f.submit "追加", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>