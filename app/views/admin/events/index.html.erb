<% provide(:title, 'イベント作成') %>
<div class="container">
  <h3>イベント追加</h3>
  <hr>
  <!-- 入力フォーム -->
  <%= render 'admin/events/event_form', url: "/admin/events" %>

  <!-- イベント表示 -->
  <div id="event_create">
    <%= render 'admin/events/event_all', events: @events %>
  </div>
</div>